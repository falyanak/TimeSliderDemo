@model Demo.Models.TimeSliderViewModel

<div class="sliders-wrapper p-3">
    <div class="mb-5">
        <label class="label-mini text-muted">VUE GLOBALE (MASTER)</label>
        <div id="masterSlider" class="mt-2"></div>
    </div>

    <div class="mb-4">
        <label class="label-mini text-muted">SÉLECTION DÉTAILLÉE</label>
        <div id="detailSlider" class="mt-2"></div>
    </div>
</div>